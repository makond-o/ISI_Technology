Chrome version should be 120 or higher.

1. set path to project folder in terminal
2. install dependencies from requirements: pip install -r requirements.txt
3. run tests via terminal with command: pytest -v